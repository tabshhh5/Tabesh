<?xml version="1.0"?>
<ruleset name="Tabesh WordPress Coding Standards">
    <description>Custom coding standards for the Tabesh plugin based on WordPress standards</description>

    <!-- What to scan -->
    <file>.</file>

    <!-- Exclude paths -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/docs/*</exclude-pattern>
    <exclude-pattern>*/languages/*</exclude-pattern>
    <exclude-pattern>*/assets/js/*.min.js</exclude-pattern>
    <exclude-pattern>*/assets/css/*.min.css</exclude-pattern>
    <exclude-pattern>*/.git/*</exclude-pattern>
    <exclude-pattern>*/tmp/*</exclude-pattern>

    <!-- How to scan -->
    <arg value="sp"/> <!-- Show sniff and progress -->
    <arg name="basepath" value="."/><!-- Strip the file paths down to the relevant bit -->
    <arg name="colors"/>
    <arg name="extensions" value="php"/>
    <arg name="parallel" value="8"/><!-- Enables parallel processing when available for faster results. -->

    <!-- Rules: Check PHP version compatibility -->
    <config name="testVersion" value="8.2-"/>

    <!-- Rules: WordPress Coding Standards -->
    <rule ref="WordPress">
        <!-- Exclude rules that conflict with modern PHP or are too strict -->
        <exclude name="WordPress.Files.FileName.InvalidClassFileName"/>
        <exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
        <exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
        <exclude name="WordPress.PHP.YodaConditions.NotYoda"/>
        <exclude name="WordPress.NamingConventions.ValidVariableName"/>
    </rule>

    <!-- Rules: WordPress Extra Standards -->
    <rule ref="WordPress-Extra">
        <!-- Allow short array syntax [] -->
        <exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
    </rule>

    <!-- Rules: WordPress Documentation Standards -->
    <rule ref="WordPress-Docs"/>

    <!-- Check for security issues -->
    <rule ref="WordPress.Security"/>

    <!-- Check for internationalization -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="tabesh"/>
            </property>
        </properties>
    </rule>

    <!-- Verify that nonces are used correctly -->
    <rule ref="WordPress.Security.NonceVerification"/>

    <!-- Check for SQL injection vulnerabilities -->
    <rule ref="WordPress.DB.PreparedSQL"/>

    <!-- Verify all data is escaped on output -->
    <rule ref="WordPress.Security.EscapeOutput"/>

    <!-- Allow for theme specific exceptions to file naming -->
    <rule ref="WordPress.Files.FileName">
        <properties>
            <property name="strict_class_file_names" value="false"/>
        </properties>
    </rule>

    <!-- Minimum PHP and WP version -->
    <config name="minimum_supported_wp_version" value="6.8"/>

    <!-- Set text domain for translations -->
    <config name="text_domain" value="tabesh"/>
</ruleset>
