# خلاصه کامل رفع مشکلات قیمت‌گذاری V2

## مشکلات شناسایی‌شده و رفع‌شده ✅

### 1. ✅ مشکل ذخیره‌سازی تنظیمات فعال/غیرفعال - رفع شد

**مشکل:**
وقتی مدیر هر دو گزینه چاپ تک‌رنگ (BW) و رنگی (Color) را برای یک نوع کاغذ غیرفعال می‌کرد (چک‌باکس‌ها را خالی می‌کرد)، تنظیمات ذخیره نمی‌شدند. دلیل این بود که متد `parse_restrictions()` فقط گزینه‌هایی را پردازش می‌کرد که در داده‌های POST ظاهر می‌شدند (حداقل یک چک‌باکس فعال داشتند).

**پیامدها:**
- گزینه‌های ممنوعه در فرم سفارش نمایش داده می‌شدند
- وضعیت توگل‌ها به درستی حفظ نمی‌شدند
- تنظیمات بعد از ذخیره به نظر می‌رسید که بازنشانی شده‌اند

**راه‌حل:**
متد `parse_restrictions()` را تغییر دادیم تا:
- تمام انواع کاغذ/صحافی/خدمات اضافی پیکربندی‌شده را از ابتدا دریافت کند
- همه گزینه‌ها را به‌صورت پیش‌فرض غیرفعال مقداردهی کند
- داده‌های POST را پردازش کرده و ترکیبات فعال را علامت‌گذاری کند
- **همه** گزینه‌ها را پردازش کرده و مشخص کند کدام‌ها ممنوع هستند

**فایل‌های تغییریافته:**
- `includes/handlers/class-tabesh-product-pricing.php`

### 2. ✅ نبود پشتیبانی از چاپ ترکیبی در فرم مشتری - رفع شد

**مشکل:**
سیستم گزینه "ترکیبی" (چاپ مختلط تک‌رنگ و رنگی) را در تنظیمات داشت، اما فرم سفارش مشتری به درستی آن را مدیریت نمی‌کرد. هر دو فیلد تعداد صفحات BW و Color همیشه نمایش داده می‌شدند، صرف‌نظر از نوع چاپ انتخابی.

**علت اصلی:**
- فرم سفارش مدیر (admin-order-form.js) قبلاً پشتیبانی از چاپ ترکیبی را داشت
- فرم سفارش مشتری (frontend.js) منطق نمایش/مخفی‌کردن فیلدها بر اساس نوع چاپ را نداشت
- بک‌اند از قبل از چاپ ترکیبی پشتیبانی می‌کرد (از page_count_bw و page_count_color جداگانه استفاده می‌کند)

**راه‌حل:**
پشتیبانی از چاپ ترکیبی را به فرم سفارش فرانت‌اند اضافه کردیم:
- متد `updatePageCountFields()` برای نمایش/مخفی‌کردن فیلدها بر اساس نوع چاپ
- برای 'سیاه و سفید': فقط فیلد BW نمایش داده می‌شود، رنگی به 0 تنظیم می‌شود
- برای 'رنگی': فقط فیلد Color نمایش داده می‌شود، BW به 0 تنظیم می‌شود
- برای 'ترکیبی': هر دو فیلد برای چاپ مختلط نمایش داده می‌شوند
- در تغییر نوع چاپ و بارگذاری اولیه فرم فراخوانی می‌شود

**فایل‌های تغییریافته:**
- `assets/js/frontend.js`

---

## نحوه کار سیستم اکنون

### گردش کار مدیر:

1. مدیر به فرم قیمت‌گذاری محصول می‌رود
2. برای هر نوع کاغذ، مدیر می‌تواند چاپ تک‌رنگ و/یا رنگی را فعال/غیرفعال کند
3. هنگام ذخیره:
   - **هر دو غیرفعال** → نوع کاغذ به‌عنوان ممنوع علامت‌گذاری می‌شود
   - **فقط BW فعال** → رنگی برای این کاغذ ممنوع علامت‌گذاری می‌شود
   - **فقط Color فعال** → تک‌رنگ برای این کاغذ ممنوع علامت‌گذاری می‌شود
   - **هر دو فعال** → چاپ ترکیبی مجاز است (هیچ‌چیز ممنوع نیست)

### گردش کار مشتری:

1. مشتری نوع چاپ را در فرم سفارش انتخاب می‌کند
2. فرم به‌صورت پویا فیلدهای مناسب را نمایش می‌دهد:
   - **'سیاه و سفید'** → فقط فیلد تعداد صفحات BW قابل مشاهده است
   - **'رنگی'** → فقط فیلد تعداد صفحات Color قابل مشاهده است
   - **'ترکیبی'** → هر دو فیلد قابل مشاهده است (کاربر تعداد جداگانه وارد می‌کند)
3. محاسبه قیمت از نرخ‌های جداگانه استفاده می‌کند:
   - `هزینه_کل = (تعداد_صفحات_bw × نرخ_bw) + (تعداد_صفحات_color × نرخ_color) + صحافی + خدمات_اضافی`

### نحوه کار چاپ ترکیبی:

سیستم اکنون کاملاً از چاپ ترکیبی (مختلط BW/Color) پشتیبانی می‌کند:
- **مدیر** هر دو توگل BW و Color را برای یک نوع کاغذ فعال می‌کند → چاپ ترکیبی مجاز است
- **کاربر** 'ترکیبی' را انتخاب می‌کند → می‌تواند هر دو تعداد صفحات BW و Color را وارد کند
- **موتور قیمت‌گذاری** هر کدام را جداگانه محاسبه کرده و جمع می‌کند

---

## تغییرات انجام‌شده

### فایل 1: `includes/handlers/class-tabesh-product-pricing.php`

پارس کردن محدودیت‌ها را برای سه حوزه رفع کردیم:

#### 1. **انواع چاپ** (خطوط 361-433):
قبل:
```php
// فقط انواع کاغذی که در POST ظاهر می‌شدند پردازش می‌شدند
foreach ($data['forbidden_print_types'] as $paper_type => $weights_data) {
    // فقط کاغذهایی با حداقل یک توگل فعال...
}
```

بعد:
```php
// تمام انواع کاغذ پیکربندی‌شده را دریافت کن
$all_paper_types = $this->get_configured_paper_types();

// همه را به‌عنوان غیرفعال مقداردهی کن
foreach (array_keys($all_paper_types) as $paper_type) {
    $enabled_combinations[$paper_type] = array(
        'bw' => false,
        'color' => false,
    );
}

// POST را پردازش کن تا فعال‌ها علامت‌گذاری شوند
// سپس همه را پردازش کن تا ممنوع‌ها مشخص شوند
```

#### 2. **گرماژ جلد** (خطوط 435-488):
همان الگو برای انواع صحافی و گرماژ جلد

#### 3. **خدمات اضافی** (خطوط 490-546):
همان الگو برای انواع صحافی و خدمات اضافی

### فایل 2: `assets/js/frontend.js`

پشتیبانی از چاپ ترکیبی را اضافه کردیم:

#### 1. **رویداد‌گیر اضافه شد** (خط 92):
```javascript
// گوش دادن به تغییرات نوع چاپ
this.$form.find('#print_type').on('change', (e) => 
    this.updatePageCountFields(e.target.value));
```

#### 2. **متد updatePageCountFields() اضافه شد** (خطوط 255-292):
```javascript
updatePageCountFields(printType) {
    if (printType === 'سیاه و سفید') {
        // فقط BW را نمایش بده
    } else if (printType === 'رنگی') {
        // فقط Color را نمایش بده
    } else if (printType === 'ترکیبی') {
        // هر دو را نمایش بده
    }
}
```

#### 3. **مقداردهی اولیه** (خطوط 61-65):
```javascript
// وضعیت اولیه فیلدها را هنگام بارگذاری تنظیم کن
const initialPrintType = this.$form.find('#print_type').val();
if (initialPrintType) {
    this.updatePageCountFields(initialPrintType);
}
```

---

## امنیت ✅

تمام تغییرات از بهترین شیوه‌های امنیتی وردپرس پیروی می‌کنند:
- ✅ پاکسازی ورودی با `sanitize_text_field()`
- ✅ اعتبارسنجی آرایه با بررسی‌های `is_array()`
- ✅ مقایسه دقیق با `===` و `in_array(..., true)`
- ✅ هیچ کوئری SQL تغییر نکرده (محدودیت‌ها در ساختار موجود ذخیره می‌شوند)
- ✅ هیچ الگوی خطرناک JavaScript نظیر `eval()`
- ✅ هیچ آسیب‌پذیری امنیتی جدیدی معرفی نشده
- ✅ تمام ورودی‌های فرم در قالب‌ها به درستی escape شده‌اند

---

## راهنمای تست

### 1. تست ماندگاری محدودیت‌ها

**تست 1: فقط تک‌رنگ فعال**
1. به فرم قیمت‌گذاری بروید
2. برای یک نوع کاغذ، فقط توگل تک‌رنگ را فعال کنید
3. ذخیره کنید
4. صفحه را reload کنید
5. ✅ انتظار: فقط تک‌رنگ فعال باقی بماند، رنگی غیرفعال باشد

**تست 2: فقط رنگی فعال**
1. برای یک نوع کاغذ، فقط توگل رنگی را فعال کنید
2. ذخیره کنید و reload کنید
3. ✅ انتظار: فقط رنگی فعال باقی بماند، تک‌رنگ غیرفعال باشد

**تست 3: هر دو غیرفعال** ⭐ (رفع شده)
1. برای یک نوع کاغذ، هر دو توگل را غیرفعال کنید
2. ذخیره کنید و reload کنید
3. ✅ انتظار: هر دو غیرفعال باقی بمانند
4. ✅ این قبلاً کار نمی‌کرد - اکنون رفع شده است

### 2. تست چاپ ترکیبی End-to-End

**گام 1: تنظیم مدیر**
1. به فرم قیمت‌گذاری محصول بروید
2. برای یک نوع کاغذ (مثلاً "تحریر 70")، **هر دو** توگل BW و Color را فعال کنید
3. قیمت‌های متفاوت برای هر کدام وارد کنید:
   - تک‌رنگ: 400 تومان
   - رنگی: 1200 تومان
4. ذخیره کنید

**گام 2: تست فرم مشتری**
1. به فرم سفارش مشتری بروید
2. نوع چاپ را انتخاب کنید:
   - اگر 'سیاه و سفید' انتخاب کنید → ✅ فقط فیلد BW نمایش داده شود
   - اگر 'رنگی' انتخاب کنید → ✅ فقط فیلد Color نمایش داده شود
   - اگر 'ترکیبی' انتخاب کنید → ✅ هر دو فیلد نمایش داده شوند

**گام 3: محاسبه قیمت ترکیبی**
1. نوع چاپ را 'ترکیبی' انتخاب کنید
2. تعداد صفحات را وارد کنید:
   - صفحات تک‌رنگ: 50
   - صفحات رنگی: 30
3. قیمت را محاسبه کنید
4. ✅ انتظار: 
   - هزینه صفحات BW = 50 × 400 = 20,000 تومان
   - هزینه صفحات Color = 30 × 1,200 = 36,000 تومان
   - جمع صفحات = 56,000 تومان (به علاوه صحافی و خدمات)

**گام 4: ثبت سفارش**
1. سفارش را ثبت کنید
2. ✅ انتظار: هر دو تعداد صفحات در دیتابیس ذخیره شوند

### 3. تست فیلتر فرانت‌اند

**تست 1: محدود کردن نوع چاپ**
1. در فرم قیمت‌گذاری، برای یک کاغذ فقط تک‌رنگ را فعال کنید
2. به فرم سفارش مشتری بروید
3. ✅ انتظار: وقتی این کاغذ انتخاب شود، فقط 'سیاه و سفید' قابل انتخاب باشد
4. 'رنگی' و 'ترکیبی' نباید کار کنند (یا غیرفعال شوند)

---

## سناریوهای استفاده واقعی

### سناریو 1: کتاب کاملاً تک‌رنگ
```
مشتری:
- نوع چاپ: 'سیاه و سفید'
- صفحات: 200

نتیجه:
- فقط فیلد BW نمایش داده می‌شود
- صفحات رنگی = 0 (خودکار)
- قیمت = 200 × نرخ_BW
```

### سناریو 2: کتاب کاملاً رنگی
```
مشتری:
- نوع چاپ: 'رنگی'
- صفحات: 100

نتیجه:
- فقط فیلد Color نمایش داده می‌شود
- صفحات تک‌رنگ = 0 (خودکار)
- قیمت = 100 × نرخ_رنگی
```

### سناریو 3: کتاب ترکیبی (محبوب در کتاب‌های درسی)
```
مشتری:
- نوع چاپ: 'ترکیبی'
- صفحات تک‌رنگ: 150 (متن اصلی)
- صفحات رنگی: 50 (تصاویر و نمودارها)

نتیجه:
- هر دو فیلد نمایش داده می‌شوند
- قیمت = (150 × نرخ_BW) + (50 × نرخ_رنگی)
- مثال: (150 × 400) + (50 × 1200) = 60,000 + 60,000 = 120,000 تومان
```

---

## مزایای این رفع مشکل

### برای مدیر:
✅ تنظیمات به درستی ذخیره و حفظ می‌شوند
✅ کنترل کامل بر اینکه کدام ترکیبات مجاز هستند
✅ می‌تواند انواع کاغذ را کاملاً غیرفعال کند
✅ تنظیمات پس از ذخیره پاک نمی‌شوند

### برای مشتری:
✅ فرم ساده‌تر و واضح‌تر - فقط فیلدهای مربوطه نمایش داده می‌شوند
✅ می‌تواند کتاب‌های ترکیبی (مختلط BW/Color) سفارش دهد
✅ محاسبه قیمت دقیق‌تر برای سفارشات ترکیبی
✅ تجربه کاربری بهتر - کمتر گیج‌کننده

### برای سیستم:
✅ محاسبات قیمت دقیق‌تر
✅ داده‌های دقیق‌تر در دیتابیس
✅ کمتر احتمال خطا در سفارشات
✅ قابلیت گزارش‌گیری بهتر (تفکیک BW و Color)

---

## نکات مهم

### ⚠️ قبل از استقرار:
1. تنظیمات موجود خود را backup بگیرید
2. در یک محیط تست امتحان کنید
3. تمام سناریوهای تست بالا را اجرا کنید

### 💡 بهترین شیوه‌ها:
1. برای هر کاغذ، حداقل یک نوع چاپ را فعال نگه دارید
2. برای کتاب‌های ترکیبی، **هر دو** BW و Color را فعال کنید
3. قیمت‌های رنگی را معمولاً 2-3 برابر تک‌رنگ تنظیم کنید
4. تست کنید که محدودیت‌ها در فرم مشتری به درستی اعمال می‌شوند

### 🎯 سناریوهای رایج:
- **کتاب‌های ادبی**: فقط تک‌رنگ
- **کتاب‌های کودک**: فقط رنگی
- **کتاب‌های درسی**: ترکیبی (متن تک‌رنگ + تصاویر رنگی)
- **مجلات**: ترکیبی
- **کاتالوگ‌ها**: رنگی یا ترکیبی

---

## خلاصه فنی

| جنبه | قبل | بعد |
|------|-----|-----|
| **ذخیره محدودیت‌ها** | ❌ گزینه‌های کاملاً غیرفعال ذخیره نمی‌شدند | ✅ همه حالات به درستی ذخیره می‌شوند |
| **چاپ ترکیبی** | ❌ فرم مشتری پشتیبانی نداشت | ✅ کاملاً پشتیبانی می‌شود |
| **نمایش فیلدها** | ❌ همیشه هر دو فیلد نمایش داده می‌شد | ✅ فقط فیلدهای مربوطه نمایش داده می‌شوند |
| **محاسبه قیمت** | ✅ صحیح بود | ✅ همچنان صحیح است |
| **امنیت** | ✅ خوب بود | ✅ همچنان خوب است |

---

## پشتیبانی

اگر سؤالی دارید یا به کمک نیاز دارید:
1. تنظیمات خود را backup بگیرید
2. در محیط تست امتحان کنید
3. لاگ‌های خطا را بررسی کنید (wp-content/debug.log)
4. با توسعه‌دهنده تماس بگیرید

---

**نسخه:** 1.0.5  
**تاریخ:** دسامبر 2024  
**وضعیت:** ✅ آماده برای استقرار
