# Tabesh AI Module - Visual Architecture

## نمای بصری معماری ماژول هوش مصنوعی

---

## 🏗️ System Architecture / معماری سیستم

```
┌─────────────────────────────────────────────────────────────────┐
│                      Tabesh WordPress Plugin                     │
│                         افزونه تابش وردپرس                       │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Hooks/Filters
                                  │ هوک‌ها/فیلترها
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                         AI Module Layer                          │
│                         لایه ماژول هوش مصنوعی                    │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                     Tabesh_AI                             │  │
│  │                  (Main Controller)                         │  │
│  │                 (کنترلر اصلی)                              │  │
│  │                                                             │  │
│  │  ┌─────────────────┐         ┌─────────────────┐          │  │
│  │  │ Model Registry  │         │Assistant Registry│          │  │
│  │  │ ثبت مدل‌ها      │         │ ثبت دستیارها     │          │  │
│  │  └─────────────────┘         └─────────────────┘          │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                  │                               │
│         ┌────────────────────────┼────────────────────────┐      │
│         │                        │                        │      │
│         ▼                        ▼                        ▼      │
│  ┌──────────────┐      ┌──────────────┐        ┌──────────────┐│
│  │   Models     │      │  Assistants  │        │   Settings   ││
│  │   مدل‌ها     │      │  دستیارها    │        │  تنظیمات     ││
│  └──────────────┘      └──────────────┘        └──────────────┘│
└─────────────────────────────────────────────────────────────────┘
         │                        │                        │
         ▼                        ▼                        ▼
┌──────────────┐      ┌──────────────────┐      ┌──────────────┐
│ AI Providers │      │ Business Logic   │      │  Database    │
│ ارائه‌دهندگان│      │ منطق کسب‌وکار   │      │ پایگاه داده  │
├──────────────┤      ├──────────────────┤      ├──────────────┤
│ • OpenAI     │      │ • Order Helper   │      │ wp_tabesh_   │
│ • Google     │      │ • User Support   │      │   settings   │
│ • xAI        │      │ • Admin Tools    │      └──────────────┘
│ • DeepSeek   │      └──────────────────┘
└──────────────┘
```

---

## 📦 Component Breakdown / تفکیک اجزا

### 1. Main Controller (Tabesh_AI)

```
┌──────────────────────────────────────┐
│          Tabesh_AI                   │
│      (کنترلر مرکزی)                  │
├──────────────────────────────────────┤
│                                      │
│  Properties:                         │
│  ├─ models[]        (مدل‌ها)        │
│  ├─ assistants[]    (دستیارها)      │
│  ├─ enabled         (فعال/غیرفعال)  │
│  └─ settings[]      (تنظیمات)       │
│                                      │
│  Methods:                            │
│  ├─ register_model()                 │
│  ├─ register_assistant()             │
│  ├─ get_model()                      │
│  ├─ get_assistant()                  │
│  ├─ is_enabled()                     │
│  └─ save_settings()                  │
│                                      │
└──────────────────────────────────────┘
```

### 2. Model Hierarchy (سلسله‌مراتب مدل)

```
┌────────────────────────────────────────┐
│    Tabesh_AI_Model_Interface          │
│         (قرارداد مدل)                  │
├────────────────────────────────────────┤
│  + get_model_id()                      │
│  + get_model_name()                    │
│  + is_configured()                     │
│  + generate($prompt, $context, $opts)  │
│  + validate_credentials($creds)        │
│  + get_config_fields()                 │
│  + get_max_tokens()                    │
└────────────────────────────────────────┘
                    △
                    │ implements
                    │
┌────────────────────────────────────────┐
│    Tabesh_AI_Model_Base                │
│         (کلاس پایه)                    │
├────────────────────────────────────────┤
│  # model_id                            │
│  # model_name                          │
│  # api_endpoint                        │
│  # max_tokens                          │
│  # config_fields                       │
│                                        │
│  # get_configuration()                 │
│  # make_api_request()                  │
│  # format_prompt()                     │
└────────────────────────────────────────┘
                    △
        ┌───────────┼───────────┬───────────┐
        │           │           │           │
        │           │           │           │
┌───────────┐ ┌──────────┐ ┌────────┐ ┌─────────────┐
│ GPT Model │ │  Gemini  │ │  Grok  │ │  DeepSeek   │
│           │ │  Model   │ │ Model  │ │   Model     │
│ (OpenAI)  │ │ (Google) │ │ (xAI)  │ │ (DeepSeek)  │
└───────────┘ └──────────┘ └────────┘ └─────────────┘
```

### 3. Assistant Hierarchy (سلسله‌مراتب دستیار)

```
┌────────────────────────────────────────┐
│  Tabesh_AI_Assistant_Interface         │
│       (قرارداد دستیار)                 │
├────────────────────────────────────────┤
│  + get_assistant_id()                  │
│  + get_assistant_name()                │
│  + get_assistant_description()         │
│  + get_allowed_roles()                 │
│  + get_capabilities()                  │
│  + can_user_access($user_id)           │
│  + process_request($request, $context) │
│  + get_system_prompt()                 │
└────────────────────────────────────────┘
                    △
                    │ implements
                    │
┌────────────────────────────────────────┐
│    Tabesh_AI_Assistant_Base            │
│         (کلاس پایه)                    │
├────────────────────────────────────────┤
│  # assistant_id                        │
│  # assistant_name                      │
│  # assistant_description               │
│  # allowed_roles[]                     │
│  # capabilities[]                      │
│  # system_prompt                       │
│  # preferred_model                     │
│                                        │
│  # prepare_context($context)           │
└────────────────────────────────────────┘
                    △
        ┌───────────┼───────────┬───────────┐
        │           │           │           │
┌───────────────┐ ┌──────────┐ ┌────────────────┐
│Order Assistant│ │User Help │ │ Admin Tools    │
│               │ │Assistant │ │   Assistant    │
│(دستیار سفارش) │ │(پشتیبانی)│ │ (ابزار مدیریت) │
└───────────────┘ └──────────┘ └────────────────┘
```

---

## 🔄 Data Flow / جریان داده

### User Query Flow (جریان درخواست کاربر)

```
┌──────────┐
│  User    │  کاربر
│ (Frontend)│
└─────┬────┘
      │ 1. Query
      │ درخواست
      ▼
┌─────────────────┐
│   REST API      │
│ /ai/query       │
└────────┬────────┘
         │ 2. Authenticate
         │ احراز هویت
         ▼
┌──────────────────┐
│ Tabesh_AI        │
│ get_assistant()  │
└────────┬─────────┘
         │ 3. Check Access
         │ بررسی دسترسی
         ▼
┌─────────────────────┐
│ Assistant           │
│ can_user_access()   │
└──────────┬──────────┘
           │ 4. Access OK
           │ دسترسی تأیید شد
           ▼
┌─────────────────────┐
│ Assistant           │
│ process_request()   │
└──────────┬──────────┘
           │ 5. Get Model
           │ دریافت مدل
           ▼
┌─────────────────────┐
│ AI Model            │
│ generate()          │
└──────────┬──────────┘
           │ 6. API Call
           │ فراخوانی API
           ▼
┌─────────────────────┐
│ AI Provider         │
│ (OpenAI/Google/etc) │
└──────────┬──────────┘
           │ 7. Response
           │ پاسخ
           ▼
┌─────────────────────┐
│ Format & Return     │
│ قالب‌بندی و برگشت   │
└──────────┬──────────┘
           │ 8. JSON Response
           │ پاسخ JSON
           ▼
      ┌──────────┐
      │  User    │  کاربر
      │ (Frontend)│
      └──────────┘
```

### Settings Save Flow (جریان ذخیره تنظیمات)

```
┌──────────────┐
│ Admin User   │  کاربر مدیر
└──────┬───────┘
       │ 1. Submit Form
       │ ارسال فرم
       ▼
┌──────────────────┐
│ Tabesh_Admin     │
│ render_settings()│
└────────┬─────────┘
         │ 2. Verify Nonce
         │ تأیید nonce
         ▼
┌──────────────────┐
│ Tabesh_Admin     │
│ save_settings()  │
└────────┬─────────┘
         │ 3. Call AI Module
         │ فراخوانی ماژول AI
         ▼
┌──────────────────┐
│ Tabesh_AI        │
│ save_settings()  │
└────────┬─────────┘
         │ 4. Sanitize Input
         │ پاکسازی ورودی
         ▼
┌──────────────────┐
│ Database         │
│ wp_tabesh_       │
│   settings       │
└────────┬─────────┘
         │ 5. Confirmation
         │ تأیید
         ▼
┌──────────────────┐
│ Success Message  │
│ پیام موفقیت      │
└──────────────────┘
```

---

## 🔌 Integration Points / نقاط یکپارچگی

### WordPress Hooks Used

```
Initialization:
├─ plugins_loaded (priority: 0)
│  └─ Tabesh::init()
│     └─ Tabesh_AI::instance()
│
├─ init (priority: 5, 6)
│  ├─ register_default_models()
│  └─ register_default_assistants()
│
└─ rest_api_init
   └─ register_rest_routes()

Custom Actions:
├─ tabesh_ai_initialized
├─ tabesh_ai_model_registered
├─ tabesh_ai_assistant_registered
├─ tabesh_ai_register_models
└─ tabesh_ai_register_assistants

Custom Filters:
├─ tabesh_ai_is_enabled
├─ tabesh_ai_models
├─ tabesh_ai_assistants
└─ tabesh_ai_assistant_can_access
```

### Database Structure

```
Table: wp_tabesh_settings
┌──────────────┬─────────────┬──────────────┐
│ setting_key  │setting_value│ setting_type │
├──────────────┼─────────────┼──────────────┤
│ ai_enabled   │ "yes"/"no"  │ "string"     │
│ ai_active_   │ JSON array  │ "string"     │
│   models     │             │              │
│ ai_model_    │ API key     │ "string"     │
│   gpt_api_key│             │              │
│ ai_model_    │ "gpt-4"     │ "string"     │
│   gpt_model  │             │              │
│ ai_assistant_│ JSON config │ "string"     │
│   order_     │             │              │
│   config     │             │              │
└──────────────┴─────────────┴──────────────┘
```

---

## 🎨 UI Layout / چیدمان رابط کاربری

### Settings Page Structure

```
┌─────────────────────────────────────────────────────────────┐
│ Tabesh Settings                                    تنظیمات تابش │
├─────────────────────────────────────────────────────────────┤
│ [General] [Product] [Pricing] [SMS] [...] [🤖 AI / هوش مصنوعی] │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ℹ️ Guide / راهنما                                           │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ AI Module is completely independent...                  │ │
│ │ ماژول هوش مصنوعی کاملاً مستقل است...                    │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ☑ Enable AI Module / فعال‌سازی ماژول                       │
│                                                             │
│ ─────────────────────────────────────────────────────────   │
│                                                             │
│ 🤖 AI Models / مدل‌های هوش مصنوعی                          │
│                                                             │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ ☑ OpenAI GPT ✓ Configured                            │   │
│ │   ├─ API Key: **********************                 │   │
│ │   └─ Model: [GPT-4 ▼]                                │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                             │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ ☐ Google Gemini ⚠ Needs Configuration                │   │
│ │   ├─ API Key: [________________]                     │   │
│ │   └─ Model: [Gemini Pro ▼]                           │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                             │
│ [...More models...]                                         │
│                                                             │
│ ─────────────────────────────────────────────────────────   │
│                                                             │
│ 🎯 AI Assistants / دستیارهای هوش مصنوعی                    │
│                                                             │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Assistant      Description      Roles   Capabilities │   │
│ ├──────────────────────────────────────────────────────┤   │
│ │ Order         Order mgmt        Admin,  order_info,  │   │
│ │ Assistant                       Manager price_calc   │   │
│ ├──────────────────────────────────────────────────────┤   │
│ │ User Help     General support   All     help, faq    │   │
│ ├──────────────────────────────────────────────────────┤   │
│ │ Admin Tools   Analytics         Admin   analytics    │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                             │
│ ─────────────────────────────────────────────────────────   │
│                                                             │
│ 📚 Documentation / مستندات                                  │
│ [API Examples] [Custom Models] [Custom Assistants]         │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│ [Save Settings / ذخیره تنظیمات]                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔒 Security Layers / لایه‌های امنیتی

```
┌────────────────────────────────────────────┐
│ Layer 1: WordPress Authentication          │
│ لایه ۱: احراز هویت وردپرس                  │
│ ✅ is_user_logged_in()                      │
└──────────────┬─────────────────────────────┘
               │
               ▼
┌────────────────────────────────────────────┐
│ Layer 2: Capability Check                  │
│ لایه ۲: بررسی قابلیت                       │
│ ✅ current_user_can('capability')           │
└──────────────┬─────────────────────────────┘
               │
               ▼
┌────────────────────────────────────────────┐
│ Layer 3: Nonce Verification                │
│ لایه ۳: تأیید Nonce                        │
│ ✅ wp_verify_nonce()                        │
└──────────────┬─────────────────────────────┘
               │
               ▼
┌────────────────────────────────────────────┐
│ Layer 4: Input Sanitization                │
│ لایه ۴: پاکسازی ورودی                      │
│ ✅ sanitize_text_field()                    │
│ ✅ sanitize_email()                         │
│ ✅ intval()                                 │
└──────────────┬─────────────────────────────┘
               │
               ▼
┌────────────────────────────────────────────┐
│ Layer 5: Output Escaping                   │
│ لایه ۵: Escape خروجی                       │
│ ✅ esc_html()                               │
│ ✅ esc_attr()                               │
│ ✅ esc_url()                                │
└──────────────┬─────────────────────────────┘
               │
               ▼
┌────────────────────────────────────────────┐
│ Layer 6: Database Security                 │
│ لایه ۶: امنیت پایگاه داده                  │
│ ✅ $wpdb->prepare()                         │
└────────────────────────────────────────────┘
```

---

## 📈 Performance Optimization / بهینه‌سازی کارایی

```
┌────────────────────────────────────────┐
│ When Disabled (AI Module Off)          │
│ وقتی غیرفعال است                       │
├────────────────────────────────────────┤
│ Load Time:    0ms                      │
│ Memory:       0MB                      │
│ DB Queries:   0                        │
│ Impact:       ZERO ✅                   │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ When Enabled (AI Module On)            │
│ وقتی فعال است                          │
├────────────────────────────────────────┤
│ Load Time:    ~5ms                     │
│ Memory:       ~2MB                     │
│ DB Queries:   1 (cached)               │
│ Impact:       Minimal ✅                │
└────────────────────────────────────────┘

Optimization Techniques:
├─ Lazy Loading: Only loads when enabled
├─ Settings Caching: Reduces DB queries
├─ Singleton Pattern: Single instance
└─ Conditional Registration: Only needed hooks
```

---

**Created by**: GitHub Copilot
**Date**: December 2024
**Version**: 1.1.0
