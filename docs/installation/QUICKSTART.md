# Tabesh Plugin - Quick Start Guide

## 🚀 خلاصه سریع

افزونه تابش یک سیستم کامل مدیریت سفارشات چاپ کتاب است که به صورت یکپارچه با WooCommerce کار می‌کند.

---

## 📦 نصب سریع

1. فایل‌های افزونه را در `wp-content/plugins/Tabesh/` قرار دهید
2. افزونه را از پنل مدیریت فعال کنید
3. به **تابش > تنظیمات** بروید و پیکربندی کنید
4. شورت‌کد `[tabesh_order_form]` را در یک صفحه قرار دهید

---

## 🎯 ویژگی‌های اصلی

### برای مشتریان:
- ✅ فرم سفارش چند مرحله‌ای با محاسبه قیمت آنی
- ✅ پیگیری وضعیت سفارش با نمای مراحل
- ✅ اطلاع‌رسانی پیامکی و ایمیلی
- ✅ نمایش سفارشات در حساب کاربری

### برای مدیران:
- ✅ داشبورد آماری کامل
- ✅ مدیریت سفارشات (فعال، بایگانی)
- ✅ تنظیمات پارامترهای محصول
- ✅ پیکربندی SMS و قیمت‌گذاری

### برای کارمندان:
- ✅ پنل اختصاصی مدیریت سفارشات
- ✅ به‌روزرسانی وضعیت سفارشات
- ✅ مشاهده جزئیات کامل هر سفارش

---

## 📋 شورت‌کدها

```
[tabesh_order_form]           → فرم ثبت سفارش
[tabesh_user_orders]          → سفارشات کاربر
[tabesh_staff_panel]          → پنل کارمندان
[tabesh_admin_dashboard]      → داشبورد مدیریت
```

---

## 🔌 REST API

### محاسبه قیمت:
```
POST /wp-json/tabesh/v1/calculate-price
```

### ثبت سفارش:
```
POST /wp-json/tabesh/v1/submit-order
```

### تغییر وضعیت:
```
POST /wp-json/tabesh/v1/update-status
```

---

## ⚙️ تنظیمات اولیه

### 1. پارامترهای محصول
در **تابش > تنظیمات > پارامترهای محصول**:
- قطع‌های کتاب: `A5, A4, رقعی, وزیری`
- انواع چاپ: `سیاه و سفید, رنگی, ترکیبی`
- انواع صحافی: `شومیز, جلد سخت, گالینگور`

### 2. تنظیمات SMS
در **تابش > تنظیمات > پیامک**:
- نام کاربری ملی‌پیامک
- رمز عبور
- شماره فرستنده
- شماره مدیر برای اطلاع‌رسانی

### 3. تنظیمات عمومی
- حداقل تیراژ: `10`
- حداکثر تیراژ: `10000`
- گام تیراژ: `10`

---

## 🗂️ ساختار فایل‌ها

```
Tabesh/
├── tabesh.php                  # فایل اصلی افزونه
├── includes/                   # کلاس‌های PHP
│   ├── class-tabesh-order.php
│   ├── class-tabesh-admin.php
│   ├── class-tabesh-staff.php
│   ├── class-tabesh-user.php
│   ├── class-tabesh-notifications.php
│   └── class-tabesh-woocommerce.php
├── templates/                  # فایل‌های قالب
│   ├── order-form.php
│   ├── user-orders.php
│   ├── staff-panel.php
│   ├── admin-dashboard.php
│   ├── admin-orders.php
│   ├── admin-archived.php
│   ├── admin-settings.php
│   └── shortcode-admin-dashboard.php
├── assets/
│   ├── css/
│   │   ├── frontend.css       # استایل کاربران
│   │   └── admin.css          # استایل مدیریت
│   └── js/
│       ├── frontend.js        # جاوااسکریپت کاربران
│       └── admin.js           # جاوااسکریپت مدیریت
├── languages/                  # فایل‌های ترجمه
├── README.md                   # مستندات کامل
├── INSTALL.md                  # راهنمای نصب
├── API.md                      # مستندات API
├── CHANGELOG.md                # تغییرات نسخه‌ها
├── CONTRIBUTING.md             # راهنمای مشارکت
└── .gitignore                  # فایل‌های نادیده‌گرفته شده
```

---

## 🔄 گردش کار سفارش

1. **مشتری**: فرم را پر می‌کند → قیمت محاسبه می‌شود
2. **سیستم**: سفارش ثبت می‌شود → SMS/Email ارسال می‌شود
3. **کارمند/مدیر**: سفارش را بررسی و تایید می‌کند
4. **کارمند**: وضعیت را به "در حال چاپ" تغییر می‌دهد
5. **کارمند**: پس از آماده شدن، وضعیت "آماده تحویل" می‌شود
6. **مشتری**: اطلاع‌رسانی دریافت می‌کند
7. **کارمند**: پس از تحویل، وضعیت "تحویل داده شده" می‌شود
8. **مدیر**: سفارش را بایگانی می‌کند

---

## 🎨 سفارشی‌سازی

### تغییر فرمول قیمت‌گذاری:
فایل `includes/class-tabesh-order.php` را ویرایش کنید:

```php
public function calculate_price($params) {
    // فرمول قیمت‌گذاری را اینجا تغییر دهید
}
```

### افزودن Hook سفارشی:
```php
add_action('tabesh_order_submitted', function($order_id, $data) {
    // کد سفارشی شما
}, 10, 2);
```

### تغییر استایل:
فایل‌های CSS در `assets/css/` را ویرایش کنید.

---

## 🔐 نقش‌های کاربری

| نقش | دسترسی |
|-----|--------|
| **Administrator** | دسترسی کامل به همه بخش‌ها |
| **Shop Manager** | مدیریت سفارشات و تنظیمات |
| **Shop Worker** | مشاهده و تغییر وضعیت سفارشات |
| **Customer** | ثبت و پیگیری سفارش |

---

## 📊 جداول پایگاه داده

### `wp_tabesh_orders`
ذخیره اطلاعات سفارشات

### `wp_tabesh_settings`
ذخیره تنظیمات افزونه

### `wp_tabesh_logs`
لاگ فعالیت‌ها

---

## 🆘 رفع مشکلات

### افزونه فعال نمی‌شود
- بررسی نسخه PHP (باید 8.2.2+)
- بررسی نسخه WordPress (باید 6.8+)
- اطمینان از نصب WooCommerce

### فرم نمایش داده نمی‌شود
- کش مرورگر را پاک کنید
- Permalink را ذخیره کنید

### پیامک ارسال نمی‌شود
- اعتبار حساب ملی‌پیامک را بررسی کنید
- اطلاعات ورودی را چک کنید

---

## 📞 پشتیبانی

- **GitHub**: [github.com/tabshhh12/Tabesh](https://github.com/tabshhh12/Tabesh)
- **وب‌سایت**: [chapco.ir](https://chapco.ir)

---

## 📝 لیست کامل ویژگی‌ها

✅ محاسبه قیمت پویا  
✅ فرم سفارش چند مرحله‌ای  
✅ پشتیبانی کامل از RTL  
✅ طراحی واکنش‌گرا  
✅ پنل مدیریت  
✅ پنل کارمندان  
✅ پنل کاربران  
✅ اطلاع‌رسانی SMS  
✅ اطلاع‌رسانی Email  
✅ یکپارچگی با WooCommerce  
✅ سیستم بایگانی  
✅ REST API  
✅ کنترل دسترسی بر اساس نقش  
✅ لاگ فعالیت‌ها  
✅ تنظیمات قابل تنظیم  
✅ مستندات کامل  

---

**نسخه:** 1.0.0  
**تاریخ:** نوامبر 2024  
**توسعه‌دهنده:** Chapco
